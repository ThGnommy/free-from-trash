export interface ICreator {
  name: string;
  email: string;
  province: string;
  photoURL: string;
  uid: string;
  score: number;
}

export interface ICreatorInfo {
  name: string;
  photoURL: string;
}
